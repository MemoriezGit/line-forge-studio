# UI Improvement Run — 2026-07-19

## Run identity

- Repository: `MemoriezGit/line-forge-studio`
- Baseline branch: `main`
- Baseline commit: `9b6e5816db6eca8f20bcdc90aa83e602d51b3996`
- Working branch: `codex/30-phase-ui-loop-2026-07-19`
- Objective: install and begin the 30-phase advanced UI improvement loop without replacing the working application.

## Baseline finding

The current canonical release is a working 2D single-file station-layout builder, not the advanced 3D release described by the target gate.

Verified from repository documentation and implementation:

- one canonical `index.html` application,
- `STATE`-driven rendering and action boundaries,
- Pointer Events placement/movement,
- selection and editor synchronization,
- duplicate/rotate/delete/nudge behavior,
- visual collision hints,
- local persistence with fallback,
- JSON import/export,
- focus preservation and focus-visible styling,
- keyboard shortcuts,
- built-in self-tests,
- offline runtime with no CDN dependency.

The 3D engine, camera system, spatial floor, transform gizmos, multi-scene model, outliner, undo/redo, command palette, and WebGL browser gate are not verified in the baseline.

## Changes installed in this run

1. Added `AGENTS.md` as the canonical cross-agent operating contract.
2. Added `docs/30-PHASE-UI-IMPROVEMENT-LOOP.md` with evidence-based phase statuses and advanced acceptance criteria.
3. Added `scripts/ui-loop-audit.mjs`, a zero-dependency protected-baseline structural audit.
4. Added `.github/workflows/ui-loop-audit.yml` to run the audit on pushes and pull requests and retain JSON evidence.
5. Updated `CLAUDE.md` so Claude Code follows the same loop as Codex.

## Protected behaviors

The following are release blockers if they regress:

- clean startup,
- action commit boundaries,
- selection/editor synchronization,
- pointer placement and movement,
- focus preservation,
- persistence and JSON portability,
- collision hints remaining informational,
- keyboard access,
- built-in self-tests,
- offline runtime.

## Phase weaknesses selected for the next safe increments

| Priority | Phase(s) | Measurable weakness | Safe next increment | Evidence required |
|---:|---|---|---|---|
| 1 | 30, 28, 27 | Structural self-tests exist, but reproducible browser and constrained-width evidence is missing. | Add browser smoke-test harness and screenshot scenarios without changing behavior. | Clean startup, no page errors, keyboard path, desktop + constrained-width screenshots. |
| 2 | 19 | No verified undo/redo history. | Add bounded snapshot history around existing `action_commit` semantics while preserving one commit per action. | Add, move, rotate, duplicate, delete, undo, redo, persistence boundary tests. |
| 3 | 25, 26 | Commands are spread across buttons and shortcuts; no searchable command layer. | Introduce a command registry that reuses existing actions before rendering a command palette. | Existing buttons/shortcuts and registry invoke identical actions; keyboard navigation test. |
| 4 | 8, 13, 21 | Current dimensions use prototype pixels instead of a versioned real-world unit model. | Design and test a schema migration to canonical inches while retaining 2D rendering compatibility. | Lossless import/export round-trip and old-save migration tests. |
| 5 | 17, 18, 20 | One saved layout only; no scenes/outliner. | Extend the renderer-neutral schema with scenes and active scene behind a migration/feature flag. | Scene create/switch/duplicate/delete and persistence tests without breaking legacy loads. |
| 6 | 3–7, 9–15, 23 | No verified 3D rendering path. | Add a locally bundled 3D renderer only after the normalized schema and protected test gates are established. | WebGL2 startup, camera, floor, placement, selection, transform, inspector, collision, and context-loss tests. |

## Rejected approach

A parallel 3D rewrite was rejected. It would fragment the source of truth, bypass existing persistence and interaction behaviors, and make phase completion impossible to verify against the last passing release.

## Current evidence state

- Structural audit code: installed.
- GitHub Actions workflow: installed; first branch run pending/being observed.
- Browser self-test output: not yet captured in this run.
- Screenshot evidence: not yet captured in this run.
- UI behavior change: intentionally deferred until the protected gate is confirmed.

## Next action

Confirm the structural audit passes on the working branch, then add browser-runtime verification as the first behavior-enabling improvement. Only after that gate passes should the run alter application behavior.

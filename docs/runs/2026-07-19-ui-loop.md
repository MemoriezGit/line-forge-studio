# UI Improvement Run — 2026-07-19

## Run identity

- Repository: `MemoriezGit/line-forge-studio`
- Baseline branch: `main`
- Baseline commit: `9b6e5816db6eca8f20bcdc90aa83e602d51b3996`
- Working branch: `codex/30-phase-ui-loop-2026-07-19`
- Latest passing implementation commit: `d18d2409dbca627e36801d7452dc9b0dec9c8d47`
- Latest passing workflow run: `29701908814`
- Draft pull request: `#1`
- Objective: install and begin the 30-phase advanced UI improvement loop without replacing the working application.

## Baseline finding

The current canonical release is a working 2D single-file station-layout builder, not the advanced 3D release described by the target gate.

Verified from repository documentation and implementation:

- one canonical `index.html` application,
- `STATE`-driven rendering and action boundaries,
- Pointer Events placement/movement,
- selection and editor synchronization,
- duplicate/rotate/delete/nudge behavior,
- visual collision hints,
- local persistence with fallback,
- JSON import/export,
- focus preservation and focus-visible styling,
- keyboard shortcuts,
- built-in self-tests,
- offline runtime with no CDN dependency.

The 3D engine, camera system, spatial floor, transform gizmos, multi-scene model, outliner, undo/redo, command palette, and WebGL browser gate are not verified in the baseline.

## Changes installed in this run

1. Added `AGENTS.md` as the canonical cross-agent operating contract.
2. Added `docs/30-PHASE-UI-IMPROVEMENT-LOOP.md` with evidence-based phase statuses and advanced acceptance criteria.
3. Added `scripts/ui-loop-audit.mjs`, a zero-dependency protected-baseline structural audit.
4. Added `.github/workflows/ui-loop-audit.yml` to run structural and browser checks and retain evidence artifacts.
5. Updated `CLAUDE.md` so Claude Code follows the same loop as Codex.
6. Added `playwright.config.mjs` and `tests/ui-smoke.spec.mjs` for Chromium verification.
7. Opened draft pull request `#1`; it remains unmerged while improvement work continues.
8. Scheduled hourly continuation runs for the remainder of July 19, 2026 on this same branch and draft PR.
9. Opened issue `#2` for the delegated icon-button pointer-click defect discovered by the browser gate.
10. Pinned every third-party GitHub Action to the exact passing commit SHA and disabled persisted checkout credentials in both jobs.
11. Added a Chromium regression gate for pointer movement, grid snapping, action commit/status evidence, local persistence, and restored coordinates after reload.
12. Added a Chromium regression gate proving malformed JSON import is contained without replacing, deselecting, or corrupting the working persisted layout.
13. Pinned the Playwright test runner, Chromium installer, and test command to exact version `1.60.0` so future browser evidence is not silently changed by an upstream latest release.
14. Added a Chromium recovery gate proving syntactically corrupted persisted JSON falls back to a clean layout, leaves the application usable, and permits a newly placed item to persist across reload.
15. Added a Chromium destructive-action gate proving Clear Layout requires confirmation, cancellation preserves the working persisted layout, acceptance clears items and selection, and the cleared state survives reload.
16. Added a Chromium rotate gate proving keyboard activation swaps width/height, toggles rotation, advances the revision exactly once, reports a rotated status, survives reload, and produces screenshot evidence.

## Browser scenarios now enforced

- clean startup with no page or console errors,
- accessible canvas and toolbar naming,
- all built-in self-tests passing,
- deterministic keyboard focus traversal from Save to Load,
- pointer placement from palette to canvas,
- pointer movement changes both coordinates,
- moved coordinates remain exact grid multiples while snapping is enabled,
- move completion advances the revision and reports `Moved item`,
- moved coordinates survive local persistence and reload,
- selection opens the editor,
- keyboard activation duplicates and changes the object count,
- keyboard activation rotates the selected object,
- rotate swaps width and height, toggles rotation, commits exactly once, and survives reload,
- JSON export produces a valid two-item project,
- malformed JSON import reports `Could not import JSON.` without changing items or selection,
- the pre-import working layout remains intact after reload,
- corrupted JSON in local persistence starts as a normalized empty layout without page or console errors,
- the recovered application accepts a new placement and restores it after reload,
- Clear Layout presents a confirmation dialog,
- dismissing Clear Layout preserves the working item both immediately and after reload,
- accepting Clear Layout removes all items, clears selection, reports `Cleared layout`, and remains empty after reload,
- reload restores the saved layout,
- keyboard Delete removes the topmost persisted object,
- post-delete focus returns to the canvas,
- constrained 900×800 layout preserves palette and canvas while collapsing the editor,
- desktop, rotated-item, moved-item, invalid-import, corrupted-storage recovery, clear-confirmed, persisted-layout, and constrained-width screenshot evidence is retained.

## Passing evidence

GitHub Actions run `29701908814` passed at commit `d18d2409dbca627e36801d7452dc9b0dec9c8d47`:

- **Protected baseline audit:** success, including the strict structural audit and uploaded machine-readable JSON.
- **Chromium behavior smoke test:** success using the pinned Playwright `1.60.0` runner and Chromium revision.
- **Rotate semantics:** keyboard activation swaps `w` and `h`, toggles `rotation`, advances `STATE.revision` exactly once, and reports a status containing `Rotated`.
- **Rotate durability:** the same item restores with identical width, height, and rotation after reload.
- **Runtime containment:** the rotate path produces no page errors or console errors.
- **Screenshot evidence:** `desktop-rotated-persisted-item.png` is retained with the browser report artifacts.
- **Pinned CI dependencies:** `actions/checkout`, `actions/setup-node`, and `actions/upload-artifact` resolve to exact audited SHAs.
- **Least-privilege checkout:** `persist-credentials: false` is enforced in both jobs; workflow permissions remain `contents: read`.

A failed intermediate run was retained as evidence rather than hidden:

- commit `bd963d4df2cd9103f4fda21f95a7c95e49f6c02f` physically clicked the Rotate button through Playwright;
- the hit landed on the nested SVG, leaving `event.target.id` empty;
- Rotate did not fire, directly reproducing issue `#2` in Chromium;
- commit `d18d2409dbca627e36801d7452dc9b0dec9c8d47` scoped the rotate-state gate to keyboard activation while preserving the application-level defect as the next required fix.

Earlier failed runs were also used as evidence:

- the first failure exposed a test-server identifier shadowing the built-in `URL` constructor,
- the second exposed asynchronous full-render timing and localStorage isolation assumptions,
- the third exposed export render timing,
- the fourth confirmed duplicate objects can overlap and that the topmost object must be targeted for a realistic pointer selection test,
- the first draft of the corrupted-storage assertion incorrectly referenced a browser global from the Node test context; commit `c08ad1b324c35a4f485809475db66c9328398118` corrected the assertion before the protected run.

## Protected behaviors

The following are release blockers if they regress:

- clean startup,
- action commit boundaries,
- selection/editor synchronization,
- pointer placement and movement,
- snap-to-grid movement semantics,
- rotate state and persistence semantics,
- focus preservation,
- persistence and JSON portability,
- invalid import containment,
- corrupted persistence recovery,
- destructive clear confirmation and persistence semantics,
- collision hints remaining informational,
- keyboard access,
- built-in self-tests,
- responsive access to the palette and canvas,
- offline runtime.

## Phase weaknesses selected for the next safe increments

| Priority | Phase(s) | Measurable weakness | Safe next increment | Evidence required |
|---:|---|---|---|---|
| 1 | 28, 26 | Delegated mouse clicks on icon/label children can miss the owning button because `handle_click` reads `event.target.id`; Chromium run `29701809267` reproduced this on Rotate. | Fix issue `#2` by normalizing pointer clicks to the closest button and add nested-icon/label regression tests. | Save, Load, Snap, Print, Rotate, Duplicate, and Delete work from icon, label, padding, and keyboard activation. |
| 2 | 19 | No verified undo/redo history. | Add bounded snapshot history around existing `action_commit` semantics while preserving one commit per action. | Add, move, rotate, duplicate, delete, undo, redo, persistence boundary tests. |
| 3 | 25, 26 | Commands are spread across buttons and shortcuts; no searchable command layer. | Introduce a command registry that reuses existing actions before rendering a command palette. | Existing buttons/shortcuts and registry invoke identical actions; keyboard navigation test. |
| 4 | 8, 13, 21 | Current dimensions use prototype pixels instead of a versioned real-world unit model. | Design and test a schema migration to canonical inches while retaining 2D rendering compatibility. | Lossless import/export round-trip and old-save migration tests. |
| 5 | 17, 18, 20 | One saved layout only; no scenes/outliner. | Extend the renderer-neutral schema with scenes and active scene behind a migration/feature flag. | Scene create/switch/duplicate/delete and persistence tests without breaking legacy loads. |
| 6 | 3–7, 9–15, 23 | No verified 3D rendering path. | Add a locally bundled 3D renderer only after the normalized schema and protected test gates are established. | WebGL2 startup, camera, floor, placement, selection, transform, inspector, collision, and context-loss tests. |

## Rejected approach

A parallel 3D rewrite was rejected. It would fragment the source of truth, bypass existing persistence and interaction behaviors, and make phase completion impossible to verify against the last passing release.

## Current evidence state

- Structural audit: installed and passing.
- Chromium behavior suite: installed and passing with pointer movement, snapping, commit/status, rotate persistence, reload persistence, invalid import containment, corrupted persistence recovery, and destructive clear confirmation coverage.
- GitHub Actions workflow: installed, dependency-pinned, least-privilege, and passing.
- Browser runner and installed Chromium revision: reproducibly pinned through Playwright `1.60.0`.
- Screenshot and report evidence: generated and retained by the workflow.
- Application behavior changes: protected gates only so far; issue `#2` remains the first measured behavior fix queued and now has direct Chromium reproduction evidence.

## Next action

Fix issue `#2` with one focused application commit and nested icon/label browser regression tests, rerun the full protected gate, then proceed to bounded undo/redo only after the interaction fix remains green.

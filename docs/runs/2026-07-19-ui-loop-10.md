# UI Improvement Run 10 — 2026-07-19

## Run identity

- Repository: `MemoriezGit/line-forge-studio`
- Working branch: `codex/30-phase-ui-loop-2026-07-19`
- Baseline head: `b2606a1e30372856df4263fd742749e162071139`
- Verification commit: `d49bd64f2101f9062639db4df9343987636c1341`
- GitHub Actions run: `29707382336`
- Draft pull request: `#1` remains open, draft, mergeable, and unmerged.

## Focused increment

Added `tests/nested-button-clicks.spec.mjs`, an executable Chromium expected-failure contract for issue `#2`.

The scenario:

1. starts the canonical `index.html` application without modifying production behavior;
2. places and selects a real palette item;
3. physically clicks the nested SVG inside the Rotate button;
4. asserts the owning Rotate action should swap dimensions, toggle rotation, advance the revision once, and report a rotated status;
5. marks the scenario as an expected failure with the exact defect reason: `handle_click` dispatches from `event.target.id` rather than the closest owning button.

This converts the previously observed failure into a permanent, reproducible contract. The suite stays green while the defect is known, and will report an unexpected pass as soon as the production fix lands so the marker must be removed and the scenario becomes a normal protected regression test.

## Exact evidence

GitHub Actions run `29707382336` completed successfully at commit `d49bd64f2101f9062639db4df9343987636c1341`:

- **Protected baseline audit:** success.
- **Chromium behavior smoke test:** success.
- The new nested-SVG scenario executed under Playwright's expected-failure semantics rather than being skipped.
- Existing protected browser scenarios remained green.

## Scope and reversibility

- No application code or persisted schema changed.
- No parallel project or renderer was created.
- The new test is isolated on port `4174`, with one worker already enforced by `playwright.config.mjs`.
- Reversal is a single-file deletion.

## Remaining risk

Issue `#2` is still an application defect. Pointer clicks on nested icon or label children can miss Save, Load, Snap, Print, Rotate, Duplicate, Delete, Export, Import, and Clear because delegated dispatch reads `event.target.id`.

## Next highest-value action

Make the production fix in `index.html`: resolve the closest owning `button[id]` once inside `handle_click`, dispatch from that button ID, preserve canvas-item selection behavior, and convert the expected-failure Rotate scenario into a normal passing test. Add equivalent physical nested-label coverage before beginning undo/redo.
